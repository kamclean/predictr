---
title: "vignette_roc"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = F); library(dplyr); library(ggplot2);library(predictr)
```

## R Markdown

```{r}
tripod_prep <- readr::read_csv("~/predictr/data/example_tripod.csv", show_col_types = F) %>%
  tripod_format(id = "score", type = "type")

tripod_plot(tripod_prep)

```




```{r}
probast_prep <- readr::read_csv("~/predictr/data/example_probast.csv", show_col_types = F) %>%
  probast_format(id = "score", type = "type")

probast_plot(probast_prep)

probast_table(probast_prep)

```

